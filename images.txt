# 方舟AI产研协同开发平台 —— 需转发到阿里云ACR的 7 个镜像
# 用法：把下面 7 行（不含注释也行）整段粘贴进 tech-shrimp/docker_image_pusher 仓库根的 images.txt，提交即触发转发。
# 备注：默认面向 amd64(x86_64) 服务器，无需 --platform；若测试服务器是 ARM 架构，再在每行后加 --platform=linux/arm64。
# pgvector/pgvector:pg16
# cassandra:5
# searxng/searxng:latest
# maven:3.9-eclipse-temurin-21
# eclipse-temurin:21-jre-alpine
# node:20-alpine
# nginx:alpine
--platform=linux/arm64 nacos/nacos-server:v3.2.2
--platform=linux/arm64 redis:7.4-alpine
--platform=linux/arm64 mysql:8.4
--platform=linux/arm64 xuxueli/xxl-job-admin:3.4.2
moby/buildkit:buildx-stable-1
eclipse-temurin:25-jre
